<% content_for :head do %>
  <% provide(:title, @title) %>
<% end %>

<% content_for :image do %>
  <div class="hill page-image">
    <%= image_tag "hill.png", class: "page-fade" %>
  </div>
<% end %>

<div class="row-fluid">
  <ul> <%= render @blogs %> </ul>
</div>
